# Index 0 is the start, index 1 is the end
# graph = [[(index to next node, (a, b, p)), (index of prior, index next, (a, b, p))]]

## Because we can guarantee that we have a diameter of 2, we know that each node is either connected to the start or end node

if __name__ == "__main__":
	pass